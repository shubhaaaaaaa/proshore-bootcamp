import MovieList from "../ui/MovieList";
import SearchBar from "../ui/SearchBar";

const Home = () => { 

  return (
    <>
      <SearchBar/>
      <MovieList/>
    </>
  );
};

export default Home;
